% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/respval_behavioral_data.R
\name{respval_behavioral_data}
\alias{respval_behavioral_data}
\title{Process behavioral observations and response validity data}
\usage{
respval_behavioral_data(dat)
}
\arguments{
\item{dat}{A \code{\link[data.table:data.table]{data.table::data.table()}} (or coercible object) containing at
minimum the following columns:
\describe{
\item{\code{NACCID}}{Participant identifier.}
\item{\code{VISITDATE}}{Visit date (coercible to character for sorting).}
\item{\verb{wadrc_c2_boc_*}}{REDCap checkbox / text columns for behavioral
observation categories. Column names must follow the pattern
\verb{wadrc_c2_boc_<variable>} or \verb{wadrc_c2_boc_<variable>___<subvar>}.}
\item{\code{respval}}{Integer code for overall response validity (1–3).}
\item{\code{loc_res___1}, ..., \code{loc_res___8}}{REDCap checkbox columns indicating
reasons for reduced response validity.}
\item{\code{respothx}}{Free-text field for "Other" response validity reason
(\code{loc_res___8}).}
}}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} returned invisibly with one row per
participant × visit × behavioral item, and the following key columns:
\describe{
\item{\code{NACCID}}{Participant identifier.}
\item{\code{VISITDATE}}{Factor with levels sorted in reverse-chronological
order.}
\item{\code{label}}{Ordered factor giving the behavioral domain (e.g.,
\code{"Mood"}, \code{"Affect"}, \code{"Response Validity"}).}
\item{\code{sublabel}}{Ordered factor giving the specific item label within
the domain (parenthetical qualifiers removed).}
\item{\code{extra_info}}{Character. Parenthetical qualifier extracted from
the original sublabel string, or \code{NA} if none was present.}
\item{\code{value}}{List column. Each element is a named list with a
\verb{$value} element (character: \code{"checked"}, \code{"unchecked"}, a validity
rating string, raw note text, or \code{NA}) and an optional \verb{$extra_info}
element (character tooltip text).}
}
}
\description{
Combines two processing pipelines into a single long-format
\code{\link[data.table:data.table]{data.table::data.table()}}:
\enumerate{
\item \strong{Behavioral observations} – columns matching \verb{wadrc_c2_boc_*} are
melted into long format, split on \verb{___} to separate the domain variable
from its sub-item index (\code{subvar}), then labelled with human-readable
\code{label} and \code{sublabel} factor columns. Parenthetical clarifications
embedded in sublabel strings are extracted into a separate \code{extra_info}
column and stripped from \code{sublabel}. Numeric REDCap codes are recoded to
\code{"checked"} / \code{"unchecked"} (or the raw text for \code{notes} fields).
\item \strong{Response validity} – columns matching \code{respval}, \verb{loc_res_*}, and
\code{respothx} are melted and labelled analogously. \code{respval} codes \code{1-3}
are recoded to \code{"Very valid"} / \code{"Questionably valid"} / \code{"Invalid"};
checkbox \verb{loc_res_*} codes become \code{"checked"} / \code{"unchecked"}. Free-text
from \code{respothx} is carried into \code{extra_info_2} for \code{loc_res___8}.
}

The two tables are row-bound with \code{\link[data.table:rbindlist]{data.table::rbindlist()}}. \code{VISITDATE},
\code{label}, and \code{sublabel} are re-levelled so that visits appear in
reverse-chronological order and domain/item groups appear in the intended
display order. Finally, \code{value} is packed into a named list column
(\code{list(value = ..., extra_info = ...)}) ready for consumption by
\code{format_date_cell()}.
}
