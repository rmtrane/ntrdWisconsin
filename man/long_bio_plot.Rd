% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/long_bio_plot.R
\name{long_bio_plot}
\alias{long_bio_plot}
\title{Longitudinal biomarker plot with reference-range bands and marginal density}
\usage{
long_bio_plot(
  dat,
  y_val = "csf_ratio_roche_ptau181_ab42_local_raw",
  dens = all_densities$csf_ratio_roche_ptau181_ab42_local_raw,
  cuts = all_cuts[[1]][name == "csf_ratio_roche_ptau181_ab42_local"],
  height = 100,
  width = 400,
  new_id = NULL
)
}
\arguments{
\item{dat}{A \code{data.table} of observations. Must contain columns \code{date},
\code{age}, and the column named by \code{y_val}. Rows with a missing value in
\code{y_val} are dropped, but the x-axis range is computed before that filtering
so that panels for different biomarkers share a common time span.}

\item{y_val}{Character. Name of the column in \code{dat} holding the biomarker
values to plot.}

\item{dens}{A density estimate for \code{y_val}, as returned by \code{\link[stats:density]{stats::density()}}
(or any list with \code{x}, \code{y}, \code{n}, and \code{bw} elements). \code{n} and \code{bw} are
surfaced in the density panel's info tooltip.}

\item{cuts}{A one-row-per-band \code{data.table} with columns \code{min_obs},
\code{max_obs}, and \code{color}. Infinite bounds are clamped to the computed upper
plot limit. \code{color} values are expected to be \code{rgba()} strings containing
the literal token \code{alpha} in the alpha position, which is substituted to
vary opacity between the dimmed and highlighted states.}

\item{height, width}{Numeric. Reserved for sizing; currently unused.}

\item{new_id}{Character or \code{NULL}. Reserved for identifying the plot
instance; currently unused.}
}
\value{
A list of two \code{plotly} objects, \code{scatter} and \code{density}. These are
intended to be passed to \code{\link[plotly:subplot]{plotly::subplot()}} rather than printed directly.
}
\description{
Builds the two panels for a single biomarker: a longitudinal scatter of
observed values over time, shaded by clinical cut-point bands, and a
marginal density curve for the same biomarker. The panels are returned
unjoined so that several biomarkers can be stacked in a single
\code{\link[plotly:subplot]{plotly::subplot()}} call by the caller.
}
\details{
The scatter panel carries three named trace groups that downstream code
depends on: \verb{band<i>} for the shaded cut-point regions, \code{obs_outer} for the
invisible trace carrying the wider (10\%) error bars, and \code{obs} for the
visible markers and their 5\% error bars. The JavaScript hover callback
locates traces by these names, so renaming them will silently break
highlighting.

Layout properties that are global to a figure - notably \code{shapes},
\code{annotations}, and any render hook - do not survive \code{\link[plotly:subplot]{plotly::subplot()}}
merging and must be applied to the combined figure by the caller. The
per-panel \code{shapes} and \code{annotations} set here on the density panel are an
exception only because their axis references (\code{yref = "y"}) are remapped
during the merge.
}
\seealso{
\code{\link[=add_row_titles]{add_row_titles()}} for adding per-row titles to the combined figure.
}
\keyword{internal}
