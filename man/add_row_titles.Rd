% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/long_bio_plot.R
\name{add_row_titles}
\alias{add_row_titles}
\title{Add per-row titles to a stacked subplot figure}
\usage{
add_row_titles(fig, titles, font_size = 13)
}
\arguments{
\item{fig}{A \code{plotly} object, typically the result of \code{\link[plotly:subplot]{plotly::subplot()}}.}

\item{titles}{Character vector of titles, one per row, in panel order.
May contain the pseudo-HTML subset plotly supports (\verb{<b>}, \verb{<i>}, \verb{<sub>},
\verb{<sup>}, \verb{<br>}, \verb{<span style>}); HTML entities are not decoded, so use
literal or \verb{\\\\u}-escaped unicode for special characters.}

\item{font_size}{Numeric. Title font size in pixels.}
}
\value{
The input figure with the title annotations appended.
}
\description{
A plotly figure has a single \code{layout.title}, so stacking several panels with
\code{\link[plotly:subplot]{plotly::subplot()}} leaves only one title. This adds one annotation per row
instead, positioned in paper coordinates derived from the built figure's
axis domains so that the number of rows need not be known in advance.
}
\details{
Rows are discovered by locating the trace named \code{obs} in each panel and
reading the upper bound of its y-axis domain, so \code{fig} must have been built
from panels produced by \code{\link[=long_bio_plot]{long_bio_plot()}}. Existing annotations are
preserved by appending rather than replacing.

Titles are shifted left of the plotting area to sit outside the y-axis tick
labels; the caller should allow a correspondingly large left margin on the
combined figure, and enough inter-row margin for the titles to clear the
panel above.
}
\keyword{internal}
