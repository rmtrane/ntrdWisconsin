% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data_table.R
\name{simulate_data_table}
\alias{simulate_data_table}
\title{Simulate a de-identified panel data.table matching an input's structure}
\usage{
simulate_data_table(
  dat,
  id_col,
  constant_cols = character(),
  date_col = NULL,
  date_parts = NULL,
  age_col = NULL,
  n_ids = NULL,
  seed = NULL
)
}
\arguments{
\item{dat}{A \code{data.table} (or coercible).}

\item{id_col}{Participant-id column. Its real visit-count-per-participant
distribution drives the panel structure; ids are replaced with synthetic
values (\code{"sim00001"}, ...). Rows with a missing \code{id_col} are dropped.}

\item{constant_cols}{Columns held constant within each synthetic id (drawn
once per id, independently per column, replicated across visits). E.g.
\code{SEX}, \code{EDUC}, \code{RACE}, \code{HANDED}. Only the columns you name are constrained.}

\item{date_col}{Optional single visit-date column; drawn per row then sorted
ascending within each id. Mutually exclusive with \code{date_parts}.}

\item{date_parts}{Optional length-3 character vector naming the year, month,
and day columns (in that order), for sources that store the visit date as
separate components (e.g. \code{c("VISITYR", "VISITMO", "VISITDAY")}). Composed
into a date, sorted within id, and decomposed back into the three columns.
Mutually exclusive with \code{date_col}.}

\item{age_col}{Optional age column; a per-id baseline plus the elapsed years
since that id's first visit, so age is coherent across the visit dates.
Requires a date representation (\code{date_col} or \code{date_parts}); without one,
age is constant within id. Whole-number source ages are rounded (kept
integer when source is integer).}

\item{n_ids}{Number of synthetic participants to generate. Default: the real
participant count (an exact 1:1 mirror of the source structure). When
supplied, per-participant visit counts are resampled from the real
distribution, letting you make smaller or larger fixtures.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A \code{data.table} with the same columns (names, order, types) as \code{dat}.
}
\description{
Simulate a de-identified panel data.table matching an input's structure
}
\examples{
\dontrun{
# Raw pull with split date components:
real <- readRDS("data-raw/real_uds_pull.rds")
fixture <- simulate_data_table(
  real,
  id_col        = "NACCID",
  constant_cols = c("SEX", "EDUC", "RACE", "HANDED"),
  date_parts    = c("VISITYR", "VISITMO", "VISITDAY"),
  age_col       = "NACCAGE",
  n_ids         = 20,
  seed          = 1
)
}
}
