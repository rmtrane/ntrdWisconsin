% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/behavioral_table.R
\name{behavioral_table}
\alias{behavioral_table}
\title{Build a behavioral tracking gt table}
\usage{
behavioral_table(for_tab, table_id = "status-tracker")
}
\arguments{
\item{for_tab}{A data frame to pass to \code{\link[gt:gt]{gt::gt()}}. Must contain a \code{label}
column (used as the row-group column), a \code{sublabel} column, and one or
more date columns whose names match \code{"20[0-9]{2}-[0-9]{2}-[0-9]{2}"}.
Date-column cells should be lists with \verb{$value} and \verb{$extra_info}
elements; sublabel cells should be lists with \verb{$value} and \verb{$extra_info}
elements.}

\item{table_id}{A single string used as the HTML \code{id} attribute of the
rendered table and as a CSS scope prefix. Defaults to
\code{"status-tracker"}.}
}
\value{
A \code{\link[shiny:reexports]{shiny::tagList()}} containing the rendered gt table wrapped in a
container \verb{<div>} and an inline \verb{<script>} that initialises Bootstrap
tooltips.
}
\description{
Renders a scrollable, sticky-column \pkg{gt} table with formatted date
columns and sublabel columns, row-group labels that stick to the left edge,
and Bootstrap tooltips that activate on overflow or when extra info is
available.
}
\seealso{
\code{\link[=format_date_cell]{format_date_cell()}}, \code{\link[=format_sublabel_cell]{format_sublabel_cell()}},
\code{\link[=wrap_sticky_rowgroup]{wrap_sticky_rowgroup()}}, \code{\link[=table_css]{table_css()}}, \code{\link[=tooltip_script]{tooltip_script()}}
}
